import { SearchUserStoriesListPipe } from './search-user-stories-list.pipe';

describe('SearchUserStoriesListPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchUserStoriesListPipe();
    expect(pipe).toBeTruthy();
  });
});
